-- 0015: один внешний ключ на цепочку продлений.
--
-- 0013 добавил одиночный FK extends_grant_id → access_grants(id) с ON DELETE SET NULL,
-- 0014 — композитный (organization_id, extends_grant_id), который покрывает его
-- полностью и запрещает увести цепочку в чужой tenant.
--
-- Одиночный оставлять нельзя: SET NULL при точечном удалении строки гранта тихо
-- разорвал бы цепочку, после чего отзыв закрыл бы не весь доступ, а продление
-- осталось бы действующим (ТЗ A03, M13). Поведение композитного ключа по
-- умолчанию (NO ACTION) осиротить цепочку не даёт.

alter table core.access_grants
  drop constraint access_grants_extends_grant_id_fkey;
